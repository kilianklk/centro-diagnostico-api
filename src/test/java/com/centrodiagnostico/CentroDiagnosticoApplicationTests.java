package com.centrodiagnostico;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Test estándar generado por start.spring.io. Si este test pasa,
 * significa que el ApplicationContext de Spring se pudo construir
 * completo: todos los @RestController, @Service y @Repository
 * se instanciaron y sus dependencias (@Autowired) se resolvieron
 * sin errores. Es la forma automatizada de comprobar el
 * Requerimiento 6 (además de mirar los logs de arranque).
 */
@SpringBootTest
class CentroDiagnosticoApplicationTests {

    @Test
    void contextLoads() {
        // Si el contexto no levanta, este test falla automáticamente.
    }
}
